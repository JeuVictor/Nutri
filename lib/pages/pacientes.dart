import 'package:flutter/material.dart';

class Pacientes extends StatelessWidget {
  const Pacientes({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Column(children: [Text(" Pacientes")]));
  }
}
